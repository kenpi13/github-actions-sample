name: talk-script
on:
  push:

jobs:
  talk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: execute script
        run: sh talk.sh --skip-confirm
        working-directory: ./scripts
   